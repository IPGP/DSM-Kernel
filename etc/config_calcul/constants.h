! Very slow debug mode or not: open many ASCII files from each node to print 
! debug information...
! ALWAYS set this flag to false and recompile the whole code before starting
! big production runs
!
!
  logical, parameter :: SLOW_DEBUG_MODE = .false. !!! . true.
!
!
!  timer
  logical, parameter :: USE_TIMER = .true.
!


